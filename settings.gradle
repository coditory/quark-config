plugins {
    id("com.gradle.enterprise").version("3.3.1")
}

rootProject.name = "configio"

gradleEnterprise {
    buildScan {
        termsOfServiceUrl = "https://gradle.com/terms-of-service"
        termsOfServiceAgree = "yes"
        // if (System.getenv("CI")?.isEmpty()) {
        //     publishAlways()
        //     tag("CI")
        // }
    }
}
